
"""

本结主要是基于双指针的解决方案

双指针从广义上来说，是指用两个变量在线性结构上遍历而解决的问题。狭义上说，

    对于数组，指两个变量在数组上相向移动解决的问题；
    对于链表，指两个变量在链表上同向移动解决的问题，也称为「快慢指针」问题。


当我们需要枚举一个数组中的元素时，当发现一个元素的递增伴随着第二个元素的递减时，
这个时候可以考虑使用左右指针将枚举的时间复杂度从 O(N^2) 减少至 O(N). (一般左右指针需要数组有序)

"""